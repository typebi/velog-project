spring:
  application:
    name: spring
  data:
    web:
      pageable:
        one-indexed-parameters: true
  jpa:
    show-sql: false
    properties:
      hibernate:
        format_sql: true
        dialect: org.hibernate.dialect.H2Dialect